<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Nuevo Usuario{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <!-- Header -->
    <header>
        <nav class="navbar navbar-light bg-light">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('app_login') }}">
                    <img src="https://i.ibb.co/8m8GYfT/Logo-Uni.jpg" alt="UNI Logo" width="50">
                </a>
                <div class="d-flex justify-content-center">
                    <a href="{{ path('app_usuariouni_index') }}" class="btn btn-outline-secondary mx-auto">Inicio</a>
                </div>
                <span class="navbar-text">
                    {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Cerrar Sesión</a>
                </span>
            </div>
        </nav>
    </header>

    <!-- Formulario -->
    <div class="container mt-5">
        <h1 class="text-center mb-4">Crear Nuevo Usuario</h1>
        <div class="row justify-content-center">
            <div class="col-md-6">
                {{ form_start(form) }}
                    <div class="mb-3">
                        {{ form_label(form.descripcion, 'Nombres', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.descripcion, {'attr': {'class': 'form-control', 'placeholder': 'Escriba los nombres'}}) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.fecha, 'Apellidos', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.fecha, {'attr': {'class': 'form-control', 'placeholder': 'Escriba los apellidos'}}) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.estado, 'Usuario', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.estado, {'attr': {'class': 'form-control', 'placeholder': 'Escriba el usuario'}}) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.hora_inicio, 'Contraseña', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.hora_inicio, {'attr': {'class': 'form-control', 'placeholder': 'Escriba la contraseña'}}) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.hora_final, 'Contraseña', {'label_attr': {'class': 'form-label'}}) }}
                        {{ form_widget(form.hora_final, {'attr': {'class': 'form-control', 'placeholder': 'Escriba la contraseña'}}) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.usuario, 'Tipo de Usuario', { 'label_attr': { 'class': 'form-label' } }) }}
                        {{ form_widget(form.usuario, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.usuario) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.medios, 'Tipo de Usuario', { 'label_attr': { 'class': 'form-label' } }) }}
                        {{ form_widget(form.medios, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.medios) }}
                    </div>
                    <div class="mb-3">
                        {{ form_label(form.medios, 'Tipo de Usuario', { 'label_attr': { 'class': 'form-label' } }) }}
                        {{ form_widget(form.medios, { 'attr': { 'class': 'form-control' } }) }}
                        {{ form_errors(form.medios) }}
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Agregar</button>
                    
                <div class="mb-3 form-check">                   
                </div>
                <div class="mb-3 form-check"> 
                </div>
                <div class="mb-3 form-check">                   
                </div>
                <div class="mb-3 form-check"> 
                </div>

                {{ form_end(form) }}
            </div>
        </div>
    </div>

    <!-- Footer -->
    <footer class="bg-light text-center py-3 fixed-bottom">
        <p>© UNI</p>
        <p>Contáctenos | Sede Central Avenida Universitaria | Teléfono: (505) 2270-1509</p>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
